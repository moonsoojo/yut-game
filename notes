change 'players' to 'clients'
input box to enter name on join

on 'let's play!'
  local storage is empty
  create game state in server
on join specific game
  if local storage is empty, nothing is populated
  else,
    if game id matches the url,
      populate it
    else, don't