camera doesn't shift in landing page, but does in game
  remove 'scrollToBottom' in message dimsBox
zoom doesn't fit the screen when mobile orientation changes from landscape to portrait
change 'players' to 'clients'


on 'let's play!'
  local storage is empty
  create game state in server
on join specific game
  if local storage is empty, nothing is populated
  else,
    if game id matches the url,
      populate it
    else, don't